# Images Directory

Add real image files here:

- **GXOMainHeadshotWebsite.JPG** - Hero headshot image (used in hero section background)
- **picgold.png** - Gold heart or logo image (used for paint splatter effects)

These images are referenced in the code as `/images/filename.ext` (Next.js serves files from `/public/` at the root path).
